Unless explicitly instructed otherwise, do not perform the implementation itself. Only provide the hints and reference materials needed for implementation.
An explicit user request to add or revise code comments, docs, or repository instructions counts as permission to edit those files directly.
When proposing implementation changes in this repository, include code comments in the suggested code at least as detailed as the current comments in `src/arch/x86_64/pic.rs`, and prefer more detailed comments when the control flow, hardware behavior, or magic values are not obvious.
